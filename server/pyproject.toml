[project]
name = "pocket-reader-server"
version = "1.0.0"
description = "Local TTS server for Pocket Reader Chrome extension using Pocket TTS"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "flask-cors>=4.0.0",
    "pocket-tts>=1.0.0",
    "numpy>=1.24.0",
]
